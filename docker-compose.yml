services:
  backend:
    build: ./backend
    env_file: .env
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload", "--reload-dir", "/app/app", "--reload-dir", "/app/scripts"]
    environment:
      DB_PATH: /data/copilot.db
      CORS_ALLOW_ORIGINS: '["http://localhost:${FRONTEND_PORT:-8080}"]'
    volumes:
      - app-data:/data
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    environment:
      VITE_API_TARGET: http://backend:8000
    depends_on:
      - backend
    ports:
      - "${FRONTEND_PORT:-8080}:5173"
    restart: unless-stopped

volumes:
  app-data:
