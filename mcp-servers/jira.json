{
  "mcpServers": {
    "ssr-jira-mcp": {
      "command": "ssr-jira-mcp",
      "env": {
        "JIRA_USER": "${JIRA_USER}",
        "JIRA_API_TOKEN": "${JIRA_API_TOKEN}"
      },
      "description": "JIRA workflow tools for 128T and Mist instances: JQL query, issue fetch, add comment, attachment listing/downloading, field metadata discovery, options search, and safety-validated field updates"
    }
  }
}
